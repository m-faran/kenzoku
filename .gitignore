node_modules/
dist/
build/
.cache/
.env
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# macOS
.DS_Store

# Native certs/keys (just in case you do local builds later)
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

plans/
.expo/

android/
ios/
